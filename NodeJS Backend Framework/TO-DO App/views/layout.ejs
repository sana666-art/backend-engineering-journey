<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <% if (typeof title !== 'undefined' && title) { %>
  <title><%= title %></title>
  <% } else { %>
  <title>To-Do App</title>
  <% } %>
  <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>📝</text></svg>">
  <link rel="stylesheet" href="/css/styles.css">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="<%= (typeof darkMode !== 'undefined' && darkMode) ? 'dark-mode' : '' %>">
  <header>
    <nav class="navbar">
      <div class="nav-container">
        <a href="/" class="nav-logo">
          <i class="fas fa-check-circle"></i>
          To-Do App
        </a>
        <div class="nav-menu">
          <% if (user) { %>
            <a href="/tasks" class="nav-link">Dashboard</a>
            <a href="/tasks/new" class="nav-link">Add Task</a>
            <button id="darkModeToggle" class="btn btn-secondary">
              <i class="fas fa-moon"></i>
            </button>
            <form action="/logout" method="POST" style="display: inline;">
              <button type="submit" class="btn btn-primary">Logout</button>
            </form>
          <% } else { %>
            <a href="/login" class="nav-link">Login</a>
            <a href="/signup" class="nav-link">Sign Up</a>
          <% } %>
        </div>
      </div>
    </nav>
  </header>

  <main class="main-content">
    <%- body %>
  </main>

  <footer class="footer">
    <p>&copy; 2023 To-Do App. Built with Node.js & Express.</p>
  </footer>

  <script src="/js/app.js"></script>
</body>
</html>
